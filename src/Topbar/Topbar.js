import React from 'react';
import styles from './Topbar.module.css';

const Topbar = () =>{

    return (
        <header>
      <nav>
        <h4 className={styles.Topbar}>Watcy</h4>
      </nav>
      </header>
    );

}

export default Topbar; 